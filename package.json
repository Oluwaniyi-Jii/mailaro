{
  "name": "mailaro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "test": "node --test",
    "check": "npm run test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
